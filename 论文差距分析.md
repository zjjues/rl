# 当前实验结果与可发表论文的差距分析

## 已有优势

- **算法 novelty 主线清晰**：I-MAPPO = Intent-conditioned Actor + Cross-Attention Critic + Potential-based Reward Shaping，三者构成了一条完整的故事线
- **消融实验较完整**：已覆盖 `eta`、`intent_dim`、`entropy_coef`、learning rate、rollout 长度、Phi 更新策略、critic 稳定性等多个维度
- **评估体系合理**：常规评估 + 拥挤场景评估 + 多档风险分档 + intent mutation 鲁棒性测试
- **MAPPO baseline 对比公平**：在同一代码框架内仅关闭 intent/shaping/attention 做对比
- **多 seed 可复现**：3 seed 实验 + 自动图表管线 + checkpoint 机制

---

## 关键不足（按严重程度排序）

### 1. Intent 向量是 mocked 的，不是真正的 LLM 输出

这是最致命的问题。论文标题叫 "Intent-driven MAPPO"，但 intent 实际上是随机生成或固定的 one-hot 向量，没有来自 LLM 的语义信息。如果审稿人问"LLM 在哪里？"，无法回答。要么必须接入真实 LLM，要么需要重新定义 intent 的来源并给出充分理由。

### 2. 只有一个自定义 toy 环境

全部实验都在 `uav-scheduling-v0` 这一个自建环境上完成。没有 MPE、SMACv2、VMAS 等标准 benchmark 的结果。单环境论文极难通过，审稿人会质疑过拟合和泛化性。

### 3. 性能差距不足以支撑"显著更好"的结论

Stage 3 长程结果：

| 指标 | I-MAPPO | MAPPO | 差距 |
|------|---------|-------|------|
| 常规碰撞率 | 2.53% | 2.40% | 0.13pp |
| 拥挤碰撞率 | 4.13% | 4.67% | 0.54pp |

差距太小，且没有统计显著性检验（置信区间、p-value）。Stage 3 中程的差距更大（6.67% vs 15.78%），但长程实验没有保持住这个幅度。

### 4. 训练不稳定性仍未解决

报告自身承认 critic loss 偏高、entropy 波动明显。一个训练不稳定的算法会让审稿人质疑方法的可靠性。

### 5. 缺少与更丰富 baselines 的对比

只比了 MAPPO。至少需要加上：

- Independent PPO
- MADDPG
- QMIX（连续版本）
- 近年的 MARL 方法如 HAPPO/HATRPO、MAT 等

### 6. 环境规模偏小（8 agents）

对于声称面向"UAV 调度"的工作，8 架无人机不足以体现 scalability。没有展示 agent 数量从 4 到 8 到 16 到 32 的扩展性实验。

---

## 建议：如果要投论文，需要补齐

| 优先级 | 事项 | 工作量估计 |
|--------|------|-----------|
| P0 | 决定 intent 来源——接入 LLM API，或重新定义 intent 为非 LLM 的"任务上下文向量"并给出理论依据 | 2-4 周 |
| P0 | 扩展到 2-3 个标准 MARL benchmark 环境 | 2-3 周 |
| P1 | 增加 2-3 个 baseline 算法对比 | 1-2 周 |
| P1 | 运行 scalability 实验（agent 数量扩展） | 1 周 |
| P2 | 统计显著性检验 | 1-2 天 |
| P2 | 解决训练稳定性（critic loss 过大） | 1-2 周 |

---

## 总结

这是一个扎实的工程基础，已经把实验管线、评估体系、消融框架都搭好了。但要从工程报告到可发表论文，至少还需要 **6-8 周**的增量工作，核心是解决 intent 来源的合理性、多环境验证、以及更充分的 baseline 对比。
