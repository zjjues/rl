You are an expert deep reinforcement learning researcher and PyTorch engineer. Please implement a custom Multi-Agent RL algorithm named "Intent-driven MAPPO (I-MAPPO)", which is an extension of the standard MAPPO algorithm, designed for a continuous UAV scheduling environment (based on PettingZoo MPE). 

Please write the PyTorch code based on the following specific architectural and algorithmic descriptions:

### 1. Global Definitions & Hyperparameters
- Number of agents (UAVs): `N = 4`
- Continuous Action Space: `action_dim = 3` (representing 3D velocity).
- Local Observation Space: `obs_dim = 18`
- Global State Space: `state_dim = 72`
- Intent Vector Space: `intent_dim = 8` (A high-level tactical vector generated by a mocked LLM).
- Discount factor `gamma = 0.99`, GAE lambda `gae_lambda = 0.95`.
- PPO clip parameter `epsilon = 0.2`.
- Potential reward coefficient `eta = 0.5`.

### 2. Network Architectures
**2.1 Actor Network (Distributed per agent):**
- Input: Concatenation of local observation `o_i` and the intent vector `I`. Total input dim = `obs_dim + intent_dim`.
- Backbone: A 3-layer MLP (Hidden dims: 256 -> 256 -> 128) with ReLU activations.
- Output: Two heads for continuous action distribution: `action_mean` and `action_log_std`.
- **Crucial Masking Logic:** The environment provides a binary spatial-temporal action mask `M` (same shape as action). Before sampling the action, apply this mask to restrict the physical bounds of `action_mean`. (e.g., if mask[j] == 0, clamp the corresponding velocity dimension to 0 or a safe range).

**2.2 Critic Network (Centralized Global Critic with Cross-Attention):**
- Inputs: 
  1. Global state `s` (shape: `batch_size x state_dim`).
  2. Intent vector `I` (shape: `batch_size x intent_dim`).
  3. Hidden features of all N agents `H_o` (shape: `batch_size x N x hidden_dim`). (You can extract these by passing `o_i` of all agents through a small feature extractor).
- **Cross-Attention Module:**
  - Query (Q): Linear projection of the Intent vector `I`.
  - Key (K) & Value (V): Linear projections of the agent hidden features `H_o`.
  - Compute Attention: `alpha = Softmax(Q * K^T / sqrt(d_k))`.
  - Global Context: `C_global = alpha * V` (Shape: `batch_size x hidden_dim`).
- **Value Output:** Concatenate global state `s` and `C_global`. Pass them through a 3-layer MLP (256 -> 256 -> 128) to output a single scalar: `State_Value`.

### 3. Reward Calculation (Potential-based Reward Shaping)
- The environment returns an extrinsic reward `R_env`.
- We define a potential function `Phi(s, I)` which computes the negative MSE distance between a feature mapping of `s` and the intent `I`.
- The intrinsic reward is calculated as: `R_intent = gamma * Phi(s_{t+1}, I) - Phi(s_t, I)`.
- Total step reward for each agent: `r_i = R_env + eta * R_intent`.

### 4. Training Loop (CTDE Framework)
Initialize replay buffer, Actor networks, and the centralized Critic network.
For `episode` in range `max_episodes`:
  1. Reset environment, get initial `s_0` and `o_0`.
  2. Mock the LLM: Randomly generate or fetch a fixed Intent vector `I` and Mask `M` for this episode.
  3. For `t` in range `max_steps`:
      a. Actors take `(o_i, I)`, compute masked actions `a_i`.
      b. Environment steps with `A = [a_1, ..., a_N]`, returns new states `s_{t+1}`, `o_{t+1}`, and `R_env`.
      c. Compute `r_i` using the potential reward shaping formula.
      d. Store transition `(s, o, A, I, r, s')` into buffer.
  4. If buffer is ready for update:
      a. Use the centralized Critic to compute Values for all states in the buffer using Cross-Attention.
      b. Compute Generalized Advantage Estimation (GAE) and TD(lambda) Returns.
      c. Update Critic by minimizing MSE loss between predicted Value and Returns.
      d. Update Actors by maximizing the PPO Clipped Surrogate Objective.
      e. Clear buffer.

Please output the complete, modular, and well-commented PyTorch code (including the Actor class, the CrossAttentionCritic class, and the main PPO training loop algorithm).