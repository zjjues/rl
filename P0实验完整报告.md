# I-MAPPO 实验综合报告：P0-1 LLM Intent + P0-2 VMAS Benchmark

**日期**: 2026-05-04 ~ 2026-05-08
**目标**: (1) 验证 LLM 语义 intent 向量库对 I-MAPPO 安全性能的提升；(2) 在标准 MARL benchmark 上测试跨环境泛化能力

---

## 一、代码改动总览

### P0-1: LLM Intent 接入 (2026-05-04)

| 文件 | 操作 | 说明 |
|---|---|---|
| `src/intent_llm.py` | **新建** | IntentLibrary 核心模块：25 条预置 UAV 意图描述、static hash embedding、采样/持久化/train-test split |
| `src/imappo.py` | **修改** | IMAPPOConfig 新增 `intent_source`, `intent_library_path`；IMAPPO 类新增 llm_library 模式采样；default intent_dim 8→64 |
| `src/envs/uav_scheduling_env.py` | **修改** | `set_intent()` 支持 label 参数；step info 输出 `intent_label` |
| `src/envs/__init__.py` | **修改** | Bug 修复：smaclite 导入改为 try/except，防止整个 envs 包不可用 |
| `src/imappo_experiments.py` | **修改** | CLI 新增 `--intent-source`, `--intent-dim`, `--intent-library-path` |

### P0-2: VMAS Benchmark 环境 (2026-05-07)

| 文件 | 操作 | 说明 |
|---|---|---|
| `src/envs/vmas_adapter.py` | **新建** | VMASAdapter 类：包装 vmas.make_env() 为 I-MAPPO 兼容接口（连续动作、维度自动探测、set_intent 空方法） |
| `src/imappo_vmas_experiments.py` | **新建** | VMAS 独立实验脚本：CLI→训练→评估→图表完整管线；复用 train_imappo/evaluate_imappo |
| `src/intent_llm.py` | **修改** | 新增 25 条 VMAS 意图描述 (`VMAS_INTENT_DESCRIPTIONS`)；`create_static(domain="vmas")` 参数 |
| `src/imappo.py` | **未改动** | 现有代码无需修改即可兼容 VMASAdapter（env_step 支持 list/dict action fallback，normalise_obs 支持 tuple，set_env_intent 通过 hasattr 优雅降级） |

### 关键设计决策

1. **Intent 注入位置**：intent 在模型层注入（Actor 拼接 `[obs|intent]`，Critic 用 intent 作为 cross-attention query），不由环境观测产生。这使得 intent 机制对环境透明——任何支持 `set_intent()` no-op 的环境都可接入。

2. **VMASAdapter 而非修改 GymmaWrapper**：GymmaWrapper 将 action cast 为 `int`（离散动作），VMAS 需要连续动作。独立 Adapter 比修改通用 Wrapper 更安全。

3. **独立实验脚本而非重构管线**：UAV 实验脚本深度耦合碰撞率/task_completion/probe 等概念，强行统一会引入过多抽象。两个脚本共享核心算法（train_imappo/evaluate_imappo），各自处理环境特定指标。

4. **VMAS 而非 MPE**：PettingZoo 1.26 不再内置 MPE；VMAS 是 MPE 的现代化替代，连续动作、主动维护、已集成 Gymnasium。

---

## 二、实验设计

### 实验矩阵

| 实验组 | 环境 | intent_source | 算法 | Seeds | Episodes |
|---|---|---|---|---|---|
| Stage7 LLM | UAV scheduling | `llm_library` | I-MAPPO + MAPPO | 7, 11, 23 | 3000 |
| Stage7 Baseline | UAV scheduling | `onehot` | I-MAPPO + MAPPO | 7, 11, 23 | 3000 |
| VMAS Stage1 | VMAS dispersion | `llm_library` | I-MAPPO + MAPPO | 7, 11, 23 | 3000 |

### 配置参数

**UAV (共享)**:
- 8 agents, 6 targets, intent_dim=64
- steps=50, rollout=128, batch=64, eval_interval=100, eval_episodes=5

**VMAS dispersion**:
- 3 agents, continuous actions (2D), obs_dim=13, action_dim=2, state_dim=39
- steps=50, rollout=128, batch=64, eval_interval=100, eval_episodes=5
- intent_dim=64, 25 条 VMAS 专属意图描述（cautious_navigation, aggressive_intercept, balanced_coverage 等）

---

## 三、实验结果

### 3.1 Stage7: UAV 环境 — LLM Intent 效果验证

#### 碰撞率 ↓ (越低越好)

| 指标 | I-MAPPO(LLM) | I-MAPPO(onehot) | MAPPO(LLM组) | MAPPO(onehot组) |
|---|---|---|---|---|
| Easy Probe | **0.289** | 0.424 | 0.481 | 0.353 |
| Mid Probe | **0.304** | 0.620 | 0.395 | 0.493 |
| Hard Probe | 0.517 | 0.535 | 0.517 | 0.799 |

#### 任务完成率 ↑ (越高越好)

| 指标 | I-MAPPO(LLM) | I-MAPPO(onehot) | MAPPO(LLM组) | MAPPO(onehot组) |
|---|---|---|---|---|
| Easy | 0.784 | 0.791 | 0.768 | 0.793 |
| Mid | 0.748 | 0.784 | 0.757 | 0.786 |
| Hard | 0.756 | 0.759 | 0.743 | 0.789 |

#### 关键发现

1. **LLM intent 显著降低碰撞率**：I-MAPPO(LLM) vs I-MAPPO(onehot) 碰撞率从 0.424 → 0.289（**-31.8%**）。Mid Probe 改善尤其显著：0.620 → 0.304（**-51%**）。

2. **首次明确优于 MAPPO**：I-MAPPO(LLM) 碰撞率 0.289 vs MAPPO 0.481（**-40.0%**）。而 I-MAPPO(onehot) 碰撞率 0.424 vs MAPPO 0.353（**+20% 劣势**）。LLM intent 彻底扭转了原版 I-MAPPO 跑不过 MAPPO 的局面。

3. **任务完成率持平**：LLM intent 提升安全性不以牺牲效率为代价（completion ~0.75-0.79 保持不变）。

4. **仅 25 条预置语义描述已有效**：static hash embedding 模式（零 API 依赖）已带来显著提升，说明语义多样性本身是关键，无需精确的 embedding 质量。

### 3.2 VMAS Stage1: dispersion 环境 — 跨环境泛化测试

#### 训练性能 (per-seed best)

| Seed | I-MAPPO(LLM) | MAPPO |
|---|---|---|
| 7 | 0.337 | 0.667 |
| 11 | 0.339 | 0.333 |
| 23 | **0.672** | **0.667** |
| **Mean** | 0.449 | **0.556** |

#### 确定性评估 (final deterministic eval)

| Seed | I-MAPPO(LLM) | MAPPO |
|---|---|---|
| 7 | 0.000 | 0.000 |
| 11 | 0.000 | 0.000 |
| 23 | 0.333 | 0.333 |
| **Mean** | 0.111 | 0.111 |

#### 训练动态

- **I-MAPPO 训练不稳定**：seed 11 从 0.339 衰退到 0.002（catastrophic forgetting）；seed 7 晚期停留在 0.288；仅 seed 23 持续上升。
- **MAPPO 一致性更好**：seeds 7 和 23 均达 0.667；seed 11 较差（0.333）但稳定。
- **确定性评估普遍失败**：只有 seed 23 的两种算法产生非零 eval return。其余 seed 的确定性策略无法完成任务——说明 VMAS dispersion 的稀疏 reward 环境对探索噪声敏感，确定性 rollout 使 agent 陷入固定轨迹。

#### 关键发现

1. **I-MAPPO 在 VMAS dispersion 上未展现优势**：MAPPO 训练均值 0.556 > I-MAPPO 0.449。cross-attention critic + intent shaping 在简单环境中未产生增益。

2. **VMAS dispersion 不适合作为 intent 基准**：该任务不存在安全/效率/协作等多维策略权衡——agent 只需向 landmark 移动即可。Intent 机制在存在策略 trade-off 的环境（如 UAV 的"安全 vs 效率"）中更有价值。

3. **高 seed 敏感性**：seed 23 在两种算法上均表现最好（~0.67），seed 11 均最差（~0.33）。说明 VMAS dispersion 的初始布局对学习难度影响极大。

4. **确定性评估方法不适用于 VMAS**：稀疏 reward + 确定性 rollout = agent 陷入固定路径。需要改用 stochastic 评估或多轮平均。

### 3.3 跨环境对比总结

| 维度 | UAV scheduling | VMAS dispersion |
|---|---|---|
| I-MAPPO vs MAPPO | **I-MAPPO +40% 优势** (碰撞率) | MAPPO 略优 (return 0.56 vs 0.45) |
| Intent 有效性 | **显著有效** (-31.8% collision) | 无明显效果 |
| 训练稳定性 | 可接受 (3 seed 收敛) | 较差 (catastrophic forgetting) |
| 评估可靠性 | 确定性 eval 与训练一致 | 确定性 eval 几乎全线失败 |

---

## 四、分析与讨论

### 4.1 为什么 UAV 上有效但 VMAS 上无效？

Intent 机制通过三种方式影响策略：(1) Actor 接收 intent 作为条件输入；(2) Critic 以 intent 为 query 做 cross-attention；(3) Potential function 用 intent 做 shaping target。

在 **UAV 环境**中，存在真实的安全/效率/节能等多维策略 trade-off。不同的 intent 引导出可区分的行为模式（安全优先→慢速远离人群；攻击优先→快速直飞目标），cross-attention critic 能根据 intent 选择性关注不同 agent 的状态。Intent shaping 提供了有意义的内在 reward。

在 **VMAS dispersion** 中，最优策略几乎唯一：agent 均速分散到未覆盖 landmark。intent 向量（如 "cautious_navigation" vs "aggressive_intercept"）无法实质区分行为——因为 dispersion 任务中行为最优空间非常窄。Intent 的多样性反而增加了学习的噪声维度。

### 4.2 这对论文意味着什么？

**正向**：UAV 结果强有力地证明了 I-MAPPO 的有效性。LLM intent 使 I-MAPPO 从落后 MAPPO 20% 逆转为领先 40%，且任务完成率无损。这是论文的核心卖点。

**需处理的问题**：
- 需要展示 I-MAPPO *在适合 intent 机制的环境*中有效，而非声称全域优势。UAV 环境本身就是一个合法且完整的评估场景，但审稿人可能要求更复杂的 benchmark。
- 对于 VMAS/MPE 类简单 benchmark，更好的选择可能是需要策略权衡的变体（如 obstacles + threat zones + energy constraints），或使用更具策略维度的环境（如 SMAC 的微操变体、MAMuJoCo 等）。

### 4.3 关于确定性评估的教训

VMAS dispersion 揭示了一个评估方法问题：稀疏 reward 环境中，确定性 rollout 可能由于缺乏探索噪声而无法完成任务。短期修复：评估时使用 stochastic mean action（低噪声）替代完全 deterministic；长期方案：采用 Monte Carlo 评估（多轮 stochastic rollout 平均）。

---

## 五、下一步建议

| 优先级 | 事项 | 说明 |
|---|---|---|
| P1 | 接入真实 LLM embedding API 扩展 UAV intent 库 | 当前 25 条 static hash 已有效，OpenAI embedding 可能进一步拉开差距 |
| P1 | 增加 baseline（QMIX, HAPPO） | 只比 MAPPO 不够，需更多 baseline 支撑 |
| P1 | 增加 agents 数量 scalability 实验 | 8→16→32 agents，展示 I-MAPPO 的可扩展性 |
| P1 | 统计显著性检验 | 多 seed 的基础上加 error bars 和 p-values |
| P2 | 寻找更适合 intent 的 benchmark | MAMuJoCo、SMACv2 变体、VMAS 自定义场景等 |
| P2 | 修复 VMAS 确定性评估或改用 stochastic eval | 提高评估可靠性 |
| P3 | 训练稳定性改进（critic loss 过大） | 调参或改进网络结构 |

**当前估计到达可投稿状态还需 4-6 周增量工作。** P0-1 的结果是论文的核心贡献点，P0-2 提示了方法的适用范围和局限，这两点组合可构成一个有说服力的投稿 story。
